# 督察APP 快速部署指南

## 🚀 方案一：一键部署脚本（推荐）

### 步骤 1：运行部署脚本
双击运行 `deploy.bat` 文件，按照提示操作。

### 步骤 2：创建 GitHub 仓库
1. 访问 https://github.com/new
2. 填写仓库名（如：`supervision-app`）
3. 选择 **Public（公开）**
4. **不要勾选**任何初始化选项
5. 点击 **Create repository**
6. 复制仓库地址（格式：`https://github.com/用户名/仓库名.git`）
7. 粘贴到脚本提示的输入框中

### 步骤 3：开启 GitHub Pages
1. 进入仓库页面，点击 **Settings**
2. 左侧菜单找到 **Pages**
3. **Source** 选择 `Deploy from a branch`
4. **Branch** 选择 `main` 分支
5. **文件夹** 选择 `/app`
6. 点击 **Save**

### 步骤 4：获取访问地址
等待 1-2 分钟后，页面会显示访问地址：
```
https://你的用户名.github.io/仓库名/index.html
```

手机预览地址（用于生成二维码）：
```
https://你的用户名.github.io/仓库名/preview.html
```

---

## 📱 方案二：使用 Vercel 部署（更简单）

### 1. 访问 Vercel
打开 https://vercel.com 并用 GitHub 账号登录

### 2. 导入项目
- 点击 **Add New** → **Project**
- 选择 **Import Git Repository**
- 如果还没有推送到 GitHub，可以直接拖拽 `app` 文件夹到 Vercel

### 3. 配置部署
- **Root Directory**: 选择 `app`
- 点击 **Deploy**

### 4. 获取地址
部署完成后会获得类似地址：
```
https://your-project.vercel.app/index.html
https://your-project.vercel.app/preview.html
```

---

## 💡 方案三：使用 Netlify（拖拽式）

### 1. 访问 Netlify
打开 https://app.netlify.com/drop

### 2. 拖拽部署
将整个 `app` 文件夹拖拽到页面上

### 3. 获取地址
自动部署完成后获得地址：
```
https://random-name.netlify.app/index.html
https://random-name.netlify.app/preview.html
```

---

## 🎯 最后一步：生成二维码

1. 打开本地的 `index.html`
2. 在页面右上角的输入框中输入部署后的 **preview.html** 地址
3. 点击 **生成二维码** 按钮
4. 二维码自动保存，下次打开自动显示

---

## 🔧 常见问题

### Q: Git 推送时要求登录？
**A:** 
- 方法1：使用 GitHub Desktop（图形界面，更简单）
- 方法2：配置 Personal Access Token
  1. GitHub 头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)
  2. Generate new token，勾选 `repo` 权限
  3. 复制 token，在推送时用作密码

### Q: 部署后页面空白？
**A:** 
- 检查 GitHub Pages 设置中的文件夹是否选择了 `/app`
- 等待 2-3 分钟让 GitHub Pages 完成部署

### Q: 二维码扫不出来？
**A:** 
- 确认输入的是 `preview.html` 的完整地址
- 确认地址是 https 开头（GitHub Pages 自动提供 HTTPS）

---

## 📦 交付说明

部署完成后，将以下内容发送给领导：

1. **本地文件**：整个 `app` 文件夹（或只发 index.html + app 文件夹）
2. **访问方式**：
   - 电脑端：直接双击打开 `index.html`
   - 手机端：扫描页面右上角的二维码

领导无需任何技术操作，双击即可查看！
